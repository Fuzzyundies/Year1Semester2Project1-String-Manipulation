String prompt = "Please enter word or phrase or sentence to check if it is a Palindrome.\nOnly use alphabetic characters and common punctuation (punctuation will not affect outcome)."; 
	//Adjust to what you're trying to check, and what is your acceptable input.
String pattern = (" ");
	//Adjust to what you want to take in for a pattern. Make sure to allow for spaces/common punctuation if it is stated on your sheet.
	//You will need to still trim the spaces and punctuation internally in your own method.
String userInput = IsValid(prompt, pattern);
if(userInput != null) 
	//if the user length == 0, that means the user hit cancel and wants to return to the main menu.
	{
		//Your Method
	}
